// Polling cadence (milliseconds).
export const ORDERS_POLL_INTERVAL_MS = 2500;
export const TRACKING_POLL_INTERVAL_MS = 3000;

// localStorage keys.
export const LS_COUNTER_KEY = 'bt.lastCounter';

// Delay before the PlaceOrder form auto-resets after a successful order.
export const ORDER_SUCCESS_RESET_MS = 6000;

// How long the "Last updated" timestamp is considered fresh (visual only).
export const CONNECTION_STALE_MS = 8000;
